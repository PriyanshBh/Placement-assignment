{
  "name": "policy-pulse-assessment",
  "version": "1.0.0",
  "private": true,
  "description": "Worker-thread policy ingestion, analytics, scheduling and server health dashboard",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node scripts/supervisor.js",
    "start:direct": "node src/server.js",
    "test": "node --test --test-concurrency=1",
    "sample": "node scripts/create-sample.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "exceljs": "^4.4.0",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "mongoose": "^8.18.0",
    "morgan": "^1.10.1",
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "mongodb-memory-server": "^10.2.0",
    "supertest": "^7.1.4"
  }
}
